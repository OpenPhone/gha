name: 'Setup Matrix'
description: 'Setup a test matrix'

inputs:
  shards:
    description: 'Number of shards'
    required: false
    default: '1'
outputs:
  shards:
    description: 'The test matrix shards'
runs:
  using: 'node20'
  main: 'index.js'
